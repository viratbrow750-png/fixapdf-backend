services:
  - type: web
    name: fixapdf-backend
    env: python
    buildCommand: pip install -r requirements.txt && apt-get update && apt-get install -y ghostscript
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
